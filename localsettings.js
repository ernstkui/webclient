var domain = 'http://test.api.dijkdata.nl/api/v0/'

var local_settings = {
    parameters_url: domain + 'parameters/?format=json',
    locations_url: domain + 'locations/?format=json',
    filters_url: domain +'',
    timeseries_url: domain + 'timeseries/?format=json',
}
